# RLS 実地テスト チェックリスト（公開前）

アプリのセキュリティは「RLS（行レベルアクセス制御）が正しく効いていること」に依存します。
公開前に、**2つのアカウント**で「他人のデータに触れないか」を実際に確認します。

## 準備

- **アカウントA**（オーナー役）：あなたの本アカウント
- **アカウントB**（他人役）：別のメールで作った別アカウント（別ブラウザ or シークレットウィンドウ推奨）
- Aで旅行を1つ作り、記録・支出を1件ずつ入れておく

各項目、**「期待結果」どおりになればRLSは正しく効いています**。1つでも違ったら、その項目を教えてください（ポリシーを直します）。

---

## テスト1：他人の旅行は見えない（最重要）

1. AでA所有の旅行を開き、URLをコピー（例 `/trips/xxxx-xxxx`）。この `xxxx` を B に伝える（コピペで渡すだけ）
2. Bでログインし、アドレスバーに同じ `/trips/xxxx` を貼って開く

**期待結果：** Bの画面は「旅行が見つかりませんでした」になる（中身・記録・支出・写真が一切見えない）。

> これが通れば、URLを知られてもデータは守られます。

---

## テスト2：招待した相手の権限（閲覧のみ）

1. Aで「仲間」タブ → 招待リンクを **「閲覧のみ」** で発行し、Bで参加
2. Bでその旅行を開く

**期待結果：**
- 記録・支出・しおりの内容は**見える**
- 「追加」「予約を探す」などの**編集ボタンが出ない**（閲覧専用）

---

## テスト3：編集権限でも「他人の投稿」は消せない

1. Aで招待リンクを **「編集可」** で発行し直し、Bで参加
2. Bで記録を1件追加 → Bは**自分の記録**を編集・削除できる
3. Aが作った記録を Bが開こうとする

**期待結果：**
- Bは自分が作った記録・支出だけ編集/削除できる
- Aが作った記録・支出には編集の導線が出ない（オーナーAは全部消せる）

---

## テスト4：無効な招待は弾かれる

1. Bで、でたらめなトークンのURL `/join#dummydummy` を開く

**期待結果：** 「このリンクは使用できません」と表示され、参加できない。

2. Aで発行したリンクを「無効化」→ そのリンクをBで開く

**期待結果：** 同じく「このリンクは使用できません」。

---

## テスト5：退出とオーナー保護

1. B（メンバー）は「仲間」タブから自分で**退出**できる
2. A（オーナー）には自分に対する「退出」ボタンが出ない

**期待結果：** オーナーは誤って自分を外せない。メンバーは自由に抜けられる。

---

## テスト6：アカウント削除は自分だけ

1. Bで設定 → アカウント削除を実行

**期待結果：** Bのアカウントと、Bが作ったデータだけが消える。Aの旅行は残り、メンバー一覧からBが消える。

---

## 補足：写真のアクセス

テスト1でBがAの旅行を開けない時点で、写真URL（署名付き）も取得できません。念のため、Bが参加していない旅行の写真は表示されないことも確認できます。

---

すべて期待どおりなら、RLS まわりは公開して問題ない状態です。
違う挙動があった項目だけ、番号と内容を教えてください。
