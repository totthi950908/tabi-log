# 旅行記録アプリ 要件定義書

**バージョン** 1.2（海外旅行対応・予約サイト送客を追加）
**作成日** 2026-07-21
**プロジェクト名（仮）** tabi-log

---

## 1. 概要

### 1.1 コンセプト

> **「行った旅を残す」と「かかったお金を把握する」を、ひとつの旅行にまとめるアプリ。**

旅行が終わったあと、写真はカメラロールに埋もれ、レシートは財布に溜まり、「あの旅いくらかかったっけ？」は誰も答えられない。
このアプリは **1回の旅行（Trip）** を単位に、訪れた場所の記録と支出を同じ場所に紐づけて保存する。友人・家族とは招待リンクで共有し、全員で書き込める。

### 1.2 推し活アプリとの関係

| 項目 | 内容 |
|---|---|
| 技術スタック | 同じ（React + TypeScript + Vite + Supabase） |
| Supabaseプロジェクト | **既存プロジェクトに相乗り**（`travel` スキーマを新設） |
| 認証 | 既存の Supabase Auth をそのまま利用 |
| データ分離 | スキーマ分離 + RLS により推し活データとは完全に独立 |
| デプロイ | 別リポジトリ・別ドメインで独立して運用 |

---

## 2. ターゲットとユースケース

### 2.1 想定ユーザー

- **主ユーザー（自分）** — 旅行の記録を残したい。旅費も把握したい。
- **副ユーザー（友人・家族）** — 招待リンクから参加し、自分が行った場所や払った金額を書き込む。

### 2.2 代表的なユースケース

1. **旅行前** — 「9月 京都旅行」を作成し、友人3人に招待リンクを送る
2. **旅行中** — 訪れた寺で写真を1枚撮ってメモを残す／昼食代を支出に記録する
3. **旅行後** — 総額と一人あたり金額を確認する／記録を振り返る
4. **長期的** — 「行った都道府県マップ」が埋まっていくのを眺める

---

## 3. スコープ

### 3.1 MVP（第1リリースに含む）

- [x] ユーザー登録・ログイン（メール + Google）
- [x] 旅行の作成・編集・削除
- [x] 旅行にカバー写真を1枚設定（クライアント側で圧縮）
- [x] 訪問記録（場所名・日時・都道府県・カテゴリ・評価・メモ）の追加
- [x] 支出記録（日付・カテゴリ・金額・支払者・メモ）の追加
- [x] 支出のカテゴリ別集計・総額・一人あたり金額の表示
- [x] 招待リンクによるメンバー招待（有効期限・権限付き）
- [x] メンバー一覧・権限変更・削除
- [x] 行った都道府県マップ（47都道府県の塗りつぶし）
- [x] **海外旅行の記録（国単位）と世界地図**
- [x] **外貨支出の記録（旅行ごとの為替レート換算）**
- [x] **航空券・ホテル予約サイトへの送客リンク（アフィリエイト）**
- [x] **しおり（旅のしおり）：予約情報と日程スケジュール**

### 3.2 将来（MVPには含めない）

- 割り勘の自動精算（「誰が誰にいくら」の最小送金計算）
- 複数枚の写真アルバム
- しおりの予定を訪問記録へワンタップ変換（旅行後の振り返り連携）
- 為替レートAPI連携による自動取得（MVPは手入力）
- 支出1件ごとの個別為替レート指定
- 外部API連携（Google Places、乗換案内）
- 記録の一般公開・SNSシェア画像生成
- CSVエクスポート
- 通知・リマインド

### 3.3 非対象（作らないと明記する）

- **アプリ内で完結する予約・発券機能**（在庫連携・決済。旅行業登録が必要になるため）
- 決済機能
- チャット機能（LINE等で十分）

> 予約については「アプリ内完結」は作らないが、外部予約サイトへの**送客リンク**（BOOK要件）は作る。両者は別物である点に注意。

---

## 4. 機能要件

### 4.1 認証（AUTH）

| ID | 要件 | 優先度 |
|---|---|---|
| AUTH-01 | メールアドレス + パスワードで登録・ログインできる | 必須 |
| AUTH-02 | Google アカウントでログインできる | 推奨 |
| AUTH-03 | ログアウトできる | 必須 |
| AUTH-04 | 未ログイン時、招待リンクを開くとログイン画面に飛び、ログイン後に自動で招待受諾へ戻る | 必須 |
| AUTH-05 | 表示名とアイコン（絵文字1文字）を設定できる | 必須 |

> **設計メモ** 招待は「ログイン必須」とする。匿名参加を許すと誰が書いた記録か分からなくなり、支出の支払者も特定できないため。

### 4.2 旅行管理（TRIP）

| ID | 要件 | 優先度 |
|---|---|---|
| TRIP-01 | 旅行を作成できる（タイトル・行き先・開始日・終了日・メモ） | 必須 |
| TRIP-02 | 旅行にカバー写真を1枚アップロードできる | 必須 |
| TRIP-03 | 旅行一覧を新しい順に表示。カバー写真・期間・記録数・総額をカード表示 | 必須 |
| TRIP-04 | 旅行を編集・削除できる（削除はオーナーのみ、確認ダイアログ必須） | 必須 |
| TRIP-05 | 旅行を「計画中 / 旅行中 / 完了」でステータス管理できる（日付から自動判定） | 推奨 |
| TRIP-06 | 予算を設定でき、支出合計との差分を表示する | 推奨 |

### 4.3 訪問記録（LOG）

| ID | 要件 | 優先度 |
|---|---|---|
| LOG-01 | 旅行に訪問記録を追加できる | 必須 |
| LOG-02 | 記録の項目：場所名 / 訪問日時 / 国 / 都道府県 / カテゴリ / 5段階評価 / メモ | 必須 |
| LOG-03 | カテゴリは 観光地・食事・宿泊・移動・買い物・温泉・その他 から選択 | 必須 |
| LOG-04 | 記録は訪問日時順に時系列で表示される | 必須 |
| LOG-05 | 記録の投稿者アイコンが表示される | 必須 |
| LOG-06 | 自分が作成した記録のみ編集・削除できる（オーナーは全て削除可） | 必須 |
| LOG-07 | 記録を支出と紐づけられる（任意） | 推奨 |
| LOG-11 | **記録ごとに写真を1枚添付できる（任意）。非公開バケットに圧縮して保存し、メンバーのみ署名付きURLで閲覧** | 必須 |

> **写真方針の変更（v1.3）** 当初は「旅行ごとに1枚のカバー写真」だったが、実装時に「訪問記録ごとに任意で1枚」へ変更した。旅の思い出は場所単位で残したいという要望による。容量は長辺1280px・WebP・約200KBに圧縮するため、記録が増えても無料枠内に収まりやすい。バケットは非公開とし、Storage の RLS でその旅行のメンバーだけが読み書きできるよう制御する（`schema_logs.sql`）。
| LOG-08 | **国は必須項目。デフォルトは日本。国を選ぶと国旗絵文字が表示される** | 必須 |
| LOG-09 | **国が「日本」のときのみ都道府県セレクトが表示される（段階的選択）** | 必須 |
| LOG-10 | **旅行作成時に主な渡航先国を設定でき、記録追加時の国の初期値になる** | 推奨 |

> **設計メモ** 国コードは ISO 3166-1 alpha-2（`JP` `US` `FR` …）で保持する。国旗絵文字はコードから算術的に生成できるため（各文字を Regional Indicator に変換）、画像もライブラリも不要。

### 4.4 支出管理（EXPENSE）

| ID | 要件 | 優先度 |
|---|---|---|
| EXP-01 | 支出を追加できる（日付・カテゴリ・金額・支払者・メモ） | 必須 |
| EXP-02 | カテゴリは 交通費・宿泊費・食費・観光費・お土産・その他 | 必須 |
| EXP-03 | 支払者はメンバーから選択（デフォルトは自分） | 必須 |
| EXP-04 | 支出一覧を日付順で表示。合計金額を常時表示 | 必須 |
| EXP-05 | カテゴリ別の内訳を円グラフまたは横棒で表示 | 必須 |
| EXP-06 | 「総額 ÷ メンバー人数」で一人あたり金額を表示 | 必須 |
| EXP-07 | 「誰がいくら払ったか」の支払者別合計を表示 | 必須 |
| EXP-08 | 予算設定時、消化率をプログレスバーで表示 | 推奨 |
| EXP-09 | 自分が作成した支出のみ編集・削除できる（オーナーは全て削除可） | 必須 |

> **設計メモ** MVPでは「誰が誰にいくら返すか」の精算計算は行わない。ただし EXP-07（支払者別合計）まで出しておけば、手動精算には十分足りる。将来 EXP-07 のデータをそのまま精算アルゴリズムに渡せる。

#### 外貨対応

| ID | 要件 | 優先度 |
|---|---|---|
| EXP-10 | 旅行ごとに「現地通貨」と「為替レート」を1組設定できる（例：USD / 1 USD = 155.00 円） | 必須 |
| EXP-11 | 支出入力時、通貨を「円」と「現地通貨」から選べる。現地通貨が未設定の旅行では通貨選択を表示しない | 必須 |
| EXP-12 | 現地通貨の支出は、旅行のレートで円換算した金額を併記する（例：`$24.50（約3,798円）`） | 必須 |
| EXP-13 | 集計・総額・一人あたり金額は、すべて円換算後の値で計算する | 必須 |
| EXP-14 | 旅行のレートを変更すると、その旅行の全支出の円換算額が一括で再計算される | 必須 |
| EXP-15 | **円換算額の近くに注意書きを常時表示する** | 必須 |
| EXP-16 | 主要通貨（USD/EUR/KRW/TWD/THB/GBP/AUD/CNY/SGD/VND）はプリセットから選択でき、それ以外は3文字コードを手入力できる | 推奨 |

**EXP-15 の注意書き文言（確定）**

> ⚠️ 円換算額は設定した為替レートに基づく概算です。クレジットカード決済の場合、カード会社の適用レートや事務手数料により実際の請求金額とは異なります。

表示場所は、支出タブの合計金額の直下と、集計タブの上部。現地通貨が設定されていない旅行では表示しない。

> **設計メモ** 為替レートを「旅行ごとに1つ」に絞ったことで、入力の手間がほぼゼロになる。帰国後にカード明細を見て実効レートが分かったら、レートを1箇所直すだけで全支出が正しくなる — 件ごとにレートを持つ設計よりむしろ実用的。

### 4.5 共有・メンバー（SHARE）

| ID | 要件 | 優先度 |
|---|---|---|
| SHARE-01 | オーナーは招待リンクを発行できる | 必須 |
| SHARE-02 | 発行時に「権限（編集可/閲覧のみ）」「有効期限」「使用回数上限」を指定できる | 必須 |
| SHARE-03 | 招待リンクは発行直後に1度だけ全文表示され、以降は再表示できない | 必須 |
| SHARE-04 | オーナーは発行済みリンクをいつでも無効化できる | 必須 |
| SHARE-05 | リンクを開いたユーザーはログイン後、旅行に参加できる | 必須 |
| SHARE-06 | メンバー一覧で各メンバーの権限を確認・変更できる（オーナーのみ） | 必須 |
| SHARE-07 | オーナーはメンバーを削除できる。メンバーは自分で退出できる | 必須 |
| SHARE-08 | オーナーは旅行から退出できない（先に権限を委譲する） | 必須 |

---

## 5. セキュリティ設計（招待リンク）

招待リンクは「URLを知っていれば誰でも入れる」仕組みなので、ここが最大の攻撃面になる。以下を必須要件とする。

### 4.6 予約サイトへの送客（BOOK）

**方針：アプリ内で予約を完結させない。** 航空券・ホテルの検索は外部サイトに送客するのみとし、在庫・決済・発券は一切扱わない。これにより旅行業登録・決済システム・返金対応が不要になり、無料プランの記録アプリとして成立する。送客先で予約が成立するとアフィリエイト報酬が得られる。

| ID | 要件 | 優先度 |
|---|---|---|
| BOOK-01 | 旅行詳細に「航空券を探す」「宿を探す」ボタンを置く | 必須 |
| BOOK-02 | ボタンは旅行の行き先・開始日・終了日・人数を検索条件として外部サイトのURLに渡す | 必須 |
| BOOK-03 | リンクは新しいタブ（`target="_blank" rel="noopener noreferrer"`）で開く | 必須 |
| BOOK-04 | 各リンクにアフィリエイトのトラッキングパラメータを付与する | 推奨 |
| BOOK-05 | 送客先は複数用意し、ユーザーが選べる（例：航空券＝Skyscanner／宿＝楽天トラベル・Booking.com） | 推奨 |
| BOOK-06 | 外部サイトへ遷移する旨と、アフィリエイトリンクである旨を明示する | 必須 |

> **法務メモ** アフィリエイト報酬を得る場合、日本の景品表示法（ステマ規制、2023年10月施行）により「広告・PR」である旨の表示が必要。また各ASP／提携先の規約（リンクの改変禁止、自己アフィリエイト禁止など）を遵守すること。BOOK-06はこの法令対応を兼ねる。

> **設計メモ** 送客先URLの組み立てルールは提携先ごとに異なるため、`src/config/bookingLinks.ts` に「行き先・日付・人数を受け取ってURLを返す関数」として外出しする。提携先の追加・変更をコード1箇所で済ませられる。まずはアフィリエイト提携なしの素のリンクで実装し、提携審査が通り次第トラッキングパラメータを足せばよい（BOOK-04を後回しにできる）。

### 4.7 しおり（旅のしおり）（PLAN）

**「記録（過去に行ったこと）」と対をなす「予定（これから行くこと）」の機能。** これから行く旅行の、予約情報と日程スケジュールを1つの旅行にまとめる。役割分担は明確にする ── 記録（logs）＝過去、しおり（plan）＝未来。

#### 予約情報（航空券・ホテルなど）

| ID | 要件 | 優先度 |
|---|---|---|
| PLAN-01 | 予約を種別ごとに登録できる：航空券 / 宿泊 / 移動 / その他 | 必須 |
| PLAN-02 | 種別に応じた項目を入力：便名・航空会社・出発地/到着地・出発/到着日時・予約番号（航空券）、ホテル名・チェックイン/アウト・場所・予約番号（宿泊） | 必須 |
| PLAN-03 | 各予約にリンク（予約ページURL）とメモを付けられる | 推奨 |
| PLAN-04 | 予約は出発日時順に一覧表示される | 必須 |
| PLAN-05 | リンクは新しいタブ（`rel="noopener noreferrer"`）で開く | 必須 |

#### 日程スケジュール

| ID | 要件 | 優先度 |
|---|---|---|
| PLAN-06 | 旅行の開始日〜終了日から「1日目・2日目…」の日タブを自動生成する | 必須 |
| PLAN-07 | 日ごとに予定を追加できる（時刻・内容・カテゴリ・場所・メモ） | 必須 |
| PLAN-08 | **時刻は任意**。入れた予定は時刻順、時刻なしは末尾に並ぶ | 必須 |
| PLAN-09 | カテゴリは 移動・食事・観光・買い物・宿泊・その他 | 推奨 |
| PLAN-10 | 予定・予約とも、編集権限を持つメンバーが追加・編集・削除できる（旅行の共有と同じ範囲） | 必須 |

> **設計メモ** 日時は datetime-local で入力し、保存時に `new Date(v).toISOString()` でUTCに変換して timestamptz へ格納、表示時は端末ローカル時刻に戻す。これで入力した時刻とズレない。日程の時刻は日付を持たない `time` 型（"HH:mm"）にして、タイムゾーン問題を回避する。

---

### 5.1 トークンの扱い

| 項目 | 仕様 | 理由 |
|---|---|---|
| トークン長 | 32バイトの暗号論的乱数を base64url エンコード（43文字） | 総当たり不可能な空間を確保 |
| 生成方法 | `crypto.getRandomValues()`（`Math.random()` は禁止） | 予測可能性を排除 |
| DB保存形式 | **SHA-256ハッシュのみ保存。平文は保存しない** | DBが漏洩してもリンクが使えない |
| 表示 | 発行直後に1度だけ表示。以降サーバーからも取得不可 | 漏洩経路を最小化 |
| URL形式 | `/join#<token>`（**クエリではなくハッシュフラグメント**） | フラグメントはRefererヘッダやサーバーログに乗らない |

### 5.2 有効期限・使用制限

- デフォルト有効期限 **7日**（1日 / 7日 / 30日 から選択）
- デフォルト使用回数上限 **10回**
- 期限切れ・上限到達・無効化済みのリンクは即座にエラーを返す
- エラーメッセージは「このリンクは使用できません」で統一し、**「期限切れ」「存在しない」を区別しない**（存在確認による探索を防ぐ）

### 5.3 受諾処理

- 招待の受諾は **`SECURITY DEFINER` の Postgres 関数（RPC）経由**で行う
  - クライアントは `travel.invites` テーブルを直接 SELECT できない（RLSで全拒否）
  - RPC 内でトークンをハッシュ化し、サーバー側で照合する
- 同一ユーザーが同じ旅行に重複参加しないよう `UNIQUE(trip_id, user_id)` で保証
- レート制限：同一ユーザーからの受諾試行は **1分あたり5回まで**（失敗回数をカウント）

### 5.4 アクセス制御（RLS）

- `travel` スキーマの**全テーブルで RLS を有効化**する（例外なし）
- 基本ポリシー：「自分がメンバーである旅行のデータのみ読める」
- 書き込みポリシー：「編集権限を持つメンバーのみ INSERT/UPDATE 可能」
- 削除ポリシー：「自分が作成した行、またはオーナーのみ DELETE 可能」
- **`anon` ロールには一切の権限を与えない**（未ログインで到達可能なのは招待受諾RPCのみ）

### 5.5 その他

- Supabase Storage のカバー写真は **`trips/{trip_id}/cover.webp`** に格納し、Storage ポリシーでメンバーのみ閲覧可とする（公開バケットにしない）
- 環境変数は `VITE_SUPABASE_URL` と `VITE_SUPABASE_ANON_KEY` のみ。**`service_role` キーはフロントエンドに絶対に置かない**
- `.env` を `.gitignore` に追加（コミット前に必ず確認）

---

## 6. データモデル

### 6.1 スキーマ方針

推し活アプリと同じ Supabase プロジェクトを使うため、**`travel` という専用スキーマ**を新設して名前空間を分ける。Supabase ダッシュボードの `Settings > API > Exposed schemas` に `travel` を追加すること。

### 6.2 テーブル定義

#### `travel.trips` — 旅行

| カラム | 型 | 制約 | 説明 |
|---|---|---|---|
| id | uuid | PK, default gen_random_uuid() | |
| owner_id | uuid | NOT NULL, FK → auth.users | 作成者 |
| title | text | NOT NULL | 例「9月 京都旅行」 |
| destination | text | | 行き先 |
| start_date | date | NOT NULL | |
| end_date | date | NOT NULL, CHECK (end_date >= start_date) | |
| cover_path | text | | Storage のパス |
| budget | integer | | 予算（**円**） |
| default_country | char(2) | default 'JP' | 主な渡航先。記録追加時の初期値 |
| local_currency | char(3) | | 現地通貨コード（例 `USD`）。NULL なら国内旅行扱い |
| exchange_rate | numeric(14,6) | CHECK (exchange_rate > 0) | 現地通貨1単位あたりの円 |
| memo | text | | |
| created_at | timestamptz | default now() | |
| updated_at | timestamptz | default now() | |

#### `travel.trip_members` — メンバー

| カラム | 型 | 制約 | 説明 |
|---|---|---|---|
| trip_id | uuid | FK → trips, ON DELETE CASCADE | |
| user_id | uuid | FK → auth.users | |
| role | text | CHECK (role IN ('owner','editor','viewer')) | 権限 |
| joined_at | timestamptz | default now() | |
| | | **PRIMARY KEY (trip_id, user_id)** | 重複参加を防止 |

#### `travel.invites` — 招待リンク

| カラム | 型 | 制約 | 説明 |
|---|---|---|---|
| id | uuid | PK | |
| trip_id | uuid | FK → trips, ON DELETE CASCADE | |
| token_hash | text | NOT NULL, UNIQUE | SHA-256ハッシュ |
| role | text | CHECK (role IN ('editor','viewer')) | 付与する権限 |
| expires_at | timestamptz | NOT NULL | |
| max_uses | integer | NOT NULL, default 10 | |
| used_count | integer | NOT NULL, default 0 | |
| revoked_at | timestamptz | | NULL なら有効 |
| created_by | uuid | FK → auth.users | |
| created_at | timestamptz | default now() | |

#### `travel.logs` — 訪問記録

| カラム | 型 | 制約 | 説明 |
|---|---|---|---|
| id | uuid | PK | |
| trip_id | uuid | FK → trips, ON DELETE CASCADE | |
| author_id | uuid | FK → auth.users | |
| place_name | text | NOT NULL | |
| visited_at | timestamptz | NOT NULL | |
| country_code | char(2) | NOT NULL, default 'JP' | ISO 3166-1 alpha-2。世界地図用 |
| prefecture | text | CHECK (country_code = 'JP' OR prefecture IS NULL) | 都道府県マップ用。日本以外は NULL |
| category | text | CHECK (観光地/食事/宿泊/移動/買い物/温泉/その他) | |
| rating | smallint | CHECK (rating BETWEEN 1 AND 5) | |
| note | text | | |
| created_at | timestamptz | default now() | |

#### `travel.expenses` — 支出

| カラム | 型 | 制約 | 説明 |
|---|---|---|---|
| id | uuid | PK | |
| trip_id | uuid | FK → trips, ON DELETE CASCADE | |
| author_id | uuid | FK → auth.users | 登録者 |
| payer_id | uuid | FK → auth.users | 支払った人 |
| spent_on | date | NOT NULL | |
| category | text | CHECK (交通費/宿泊費/食費/観光費/お土産/その他) | |
| amount | numeric(14,2) | NOT NULL, CHECK (amount > 0) | 入力された通貨での金額 |
| currency | char(3) | NOT NULL, default 'JPY' | `JPY` または旅行の `local_currency` |
| memo | text | | |
| log_id | uuid | FK → logs, ON DELETE SET NULL | 訪問記録との紐づけ（任意） |
| created_at | timestamptz | default now() | |

> **重要** `amount` は `integer` ではなく `numeric(14,2)` にすること。USD の `12.34` のような小数を持つ通貨があるため。円の場合も小数第2位まで格納できるが、UIでは整数に丸めて表示する。浮動小数点型（`float` / `double precision`）は金額に使わない — 丸め誤差で合計が合わなくなる。

**円換算額は保存しない（算出する）**

`expenses` に円換算額のカラムは持たない。旅行のレートを変更したときに全行を更新する必要が出て、更新漏れがバグになるため。代わりにビューで算出する：

```sql
create view travel.expenses_jpy as
select
  e.*,
  case
    when e.currency = 'JPY' then e.amount
    else round(e.amount * coalesce(t.exchange_rate, 1))
  end as amount_jpy
from travel.expenses e
join travel.trips t on t.id = e.trip_id;
```

> **注意** ビューは作成者の権限で動くため、`security_invoker = true` を付けて呼び出し元のRLSを適用させること（付けないと他人の支出が見えてしまう）。
> `create view travel.expenses_jpy with (security_invoker = true) as ...`

#### `travel.profiles` — プロフィール

| カラム | 型 | 説明 |
|---|---|---|
| user_id | uuid | PK, FK → auth.users |
| display_name | text | 表示名 |
| emoji | text | アイコン代わりの絵文字1文字 |

> **設計メモ** アイコンを画像ではなく絵文字にすることで、Storage 容量とアップロード実装をまるごと省略できる。

### 6.3 RLS実装上の注意（重要）

`trip_members` のポリシーで `trip_members` 自身を参照すると **無限再帰エラー** になる。これは Supabase で最も頻出する落とし穴。

対策として、以下の `SECURITY DEFINER` 関数を用意し、全ポリシーからこれを呼ぶ：

```sql
create or replace function travel.is_member(p_trip_id uuid)
returns boolean
language sql
security definer
set search_path = travel, public
stable
as $$
  select exists (
    select 1 from travel.trip_members
    where trip_id = p_trip_id and user_id = auth.uid()
  );
$$;
```

同様に `travel.can_edit(p_trip_id)`（role が owner または editor）と `travel.is_owner(p_trip_id)` も用意する。

**もうひとつの落とし穴：旅行作成直後のアクセス不能**

`trips` の SELECT ポリシーを `is_member(id)` にすると、旅行を作成した直後は `trip_members` に行がないため、**作成者本人が自分の旅行を読めなくなる**。対策としてトリガーで自動的にオーナーをメンバー登録する：

```sql
create or replace function travel.add_owner_as_member()
returns trigger
language plpgsql
security definer
set search_path = travel, public
as $$
begin
  insert into travel.trip_members (trip_id, user_id, role)
  values (new.id, new.owner_id, 'owner');
  return new;
end;
$$;

create trigger trg_add_owner_as_member
after insert on travel.trips
for each row execute function travel.add_owner_as_member();
```

**招待受諾RPCの権限設定**

`travel.accept_invite(token text)` は `security definer` かつ `grant execute to authenticated` とする。`anon` には付与しない（AUTH-04 の通り、未ログイン時はログイン画面に誘導してから実行する）。

### 6.4 インデックス

```sql
create index on travel.trip_members (user_id);
create index on travel.logs (trip_id, visited_at desc);
create index on travel.expenses (trip_id, spent_on desc);
create index on travel.invites (token_hash);
```

---

## 7. 画面構成

```
/                    ホーム（旅行一覧）
/login               ログイン / 新規登録
/trips/new           旅行作成
/trips/:id           旅行詳細
  ├ ?tab=logs        訪問記録タブ（デフォルト）
  ├ ?tab=expenses    支出タブ
  ├ ?tab=summary     集計タブ
  └ ?tab=members     メンバータブ
/trips/:id/edit      旅行編集
/map                 行った都道府県マップ
/join#<token>        招待受諾
/settings            設定（表示名・絵文字・ログアウト）
```

### 7.1 主要画面の仕様

**ホーム**
旅行カードを縦に並べる。カードにはカバー写真（未設定なら単色グラデーション）、タイトル、期間、記録数、支出総額、メンバーの絵文字を表示。右下に FAB で「＋新しい旅行」。

**旅行詳細 — 記録タブ**
訪問日時順のタイムライン。日付ごとに区切り線を入れる。各カードに場所名・カテゴリバッジ・星評価・メモ・投稿者絵文字。右下 FAB で記録追加。

**旅行詳細 — 支出タブ**
画面上部に「総額 / 一人あたり」を大きく固定表示。その下に日付順の支出リスト。右下 FAB で支出追加。

**旅行詳細 — 集計タブ**
カテゴリ別内訳（横棒グラフ）、支払者別合計、予算消化バー。

**旅行詳細 — 予約導線**
記録タブの上部（旅行が「計画中」ステータスのとき目立つ位置）に「✈️ 航空券を探す」「🏨 宿を探す」ボタンを置く。タップすると行き先・日付・人数を渡した状態で外部サイトが新しいタブで開く。ボタン付近に小さく「外部サイトへ移動します（PR）」と明記する。

**マップ**
上部に「日本 / 世界」のタブを置く。

- **日本タブ** — 47都道府県のSVGを、訪問済みなら塗りつぶす。下部に「◯／47 都道府県」の達成率。
- **世界タブ** — 世界地図SVGを、訪問済みの国だけ塗りつぶす。下部に「◯ か国」と、訪問した国を国旗絵文字＋国名のリストで表示。

都道府県・国をタップすると、そこで記録した訪問ログの一覧が開く。

### 7.2 デザイン方針

- **モバイルファースト**（旅行中にスマホで入力するのが主用途）
- 下部固定タブバー（ホーム / マップ / 設定）
- 主要アクションは親指の届く右下 FAB に配置
- 配色：旅の空をイメージした青〜ティール系。カテゴリごとにアクセントカラーを割り当てる
- PWA 対応（ホーム画面に追加可能、オフラインでも閲覧のみ可）

---

## 8. 技術スタック

| 領域 | 採用技術 | 選定理由 |
|---|---|---|
| フレームワーク | React 18 + TypeScript | 推し活アプリと同一。知見を再利用できる |
| ビルド | Vite | 同上 |
| ルーティング | React Router v6 | |
| スタイリング | Tailwind CSS | |
| バックエンド | Supabase（既存プロジェクトの `travel` スキーマ） | 追加コストゼロ、既存Authを再利用 |
| 画像保存 | Supabase Storage（非公開バケット） | カバー写真のみ、圧縮して格納 |
| 画像圧縮 | `browser-image-compression` | アップロード前にクライアント側で圧縮 |
| グラフ | Recharts | |
| 地図 | 日本地図SVG + 世界地図SVG（自前） | 外部API・APIキー不要、完全無料 |
| 国旗 | 絵文字（国コードから算術生成） | 画像・ライブラリ不要 |
| ホスティング | Vercel または Cloudflare Pages | 無料枠で十分 |
| バージョン管理 | GitHub（新規リポジトリ） | 推し活アプリとは分離 |

### 8.1 無料枠の見積もり

| リソース | 無料枠 | 想定使用量 | 判定 |
|---|---|---|---|
| DB容量 | 500MB | 旅行100件 + 記録2,000件 + 支出2,000件 ≒ 5MB | ◎ 余裕 |
| Storage | 1GB | カバー写真1枚300KB × 100旅行 = 30MB | ◎ 余裕 |
| 帯域 | 5GB/月 | 数人利用なら数十MB/月 | ◎ 余裕 |
| MAU | 50,000 | 数人 | ◎ 余裕 |

> **注意** 推し活アプリと容量を共有するため、両方の合計で判定すること。カバー写真は **長辺1200px / WebP / 品質80** に圧縮すれば1枚300KB以下に収まる。

### 8.2 無料プランでの収益化と運用上の注意

Supabaseは**無料プランでの商用利用・収益化を明示的に許可**している。アフィリエイト報酬を得ても規約違反にはならない。壁になるのは規約ではなく以下の運用制約：

| 制約 | 内容 | 対策 |
|---|---|---|
| **7日間の無操作で一時停止** | プロジェクトに7日間アクセスがないと自動でpauseされ、次回アクセス時に手動再開が必要 | 数人で日常的に使えば起きない。心配なら1日1回の軽いクエリで生存維持（Supabase の Cron / 外部の無料 Cron から叩く） |
| DB 500MB / Storage 1GB | 推し活アプリと合算。テキスト中心なので当分余裕 | 逼迫したら古い旅行のカバー写真から削除、または Pro（$25/月）へ |
| MAU 50,000人 | 数人利用では無関係。一般公開して伸びたら壁になる | ユーザーが増えてから Pro を検討（収益が出てからでよい） |

> **順番の整理** 「収益が出たら有料化」ではなく「**ユーザーが増えたら有料化**」。友人と使う＋アフィリエイトの段階では、無料プランのまま問題なく回る。

---

## 9. 非機能要件

| 項目 | 要件 |
|---|---|
| パフォーマンス | 初回表示3秒以内。旅行一覧は写真を遅延読み込み |
| 対応環境 | iOS Safari / Android Chrome / PC Chrome の最新2バージョン |
| オフライン | 閲覧のみ可（Service Worker でキャッシュ）。書き込みは要オンライン |
| データ保全 | 削除は確認ダイアログ必須。旅行削除は関連データも CASCADE 削除 |
| エラー処理 | 通信失敗時はトーストで通知し、入力内容は保持する |
| アクセシビリティ | フォントサイズ最小14px、コントラスト比 4.5:1 以上 |

---

## 10. 開発ステップ

| # | ステップ | 内容 | 目安 |
|---|---|---|---|
| 1 | 環境構築 | Vite + React + TS + Tailwind、GitHubリポジトリ作成、Supabase接続 | 半日 |
| 2 | DB構築 | `travel` スキーマ・全テーブル・ヘルパー関数・RLSポリシーをSQLで作成 | 半日 |
| 3 | 認証 | ログイン / 新規登録 / ログアウト / プロフィール設定 | 半日 |
| 4 | 旅行CRUD | 一覧・作成・編集・削除（写真なしで先に動かす） | 1日 |
| 5 | 訪問記録 | 記録の追加・一覧・編集・削除 | 1日 |
| 6 | 支出管理 | 支出の追加・一覧・集計・グラフ | 1日 |
| 7 | 招待・共有 | 招待リンク発行 / 受諾RPC / メンバー管理 | 1日 |
| 8 | 写真 | カバー写真のアップロード・圧縮・表示 | 半日 |
| 9 | マップ | 都道府県SVG・世界地図SVG・タブ切替・達成率表示 | 1日 |
| 10 | 予約導線 | 送客リンクの設定ファイル（`bookingLinks.ts`）とボタン。まずは提携なしの素リンクで実装 | 半日 |
| 11 | 仕上げ | PWA設定・デザイン調整・デプロイ | 1日 |

> **アフィリエイト提携は開発と並行して進める。** 各ASP（A8.net、バリューコマース等）や楽天・Booking.comの提携審査には数日〜数週間かかることがある。ステップ10は提携なしの素リンクで完成させ、審査が通り次第トラッキングパラメータ（BOOK-04）を足せばよい。審査の有無に関わらず送客導線としては先に機能する。

**ステップ4まで終われば「動くもの」として触れる状態になる。** ここで一度使ってみて、以降の優先順位を見直すことを推奨する。

---

## 11. 未決事項

以下は実装を進めながら決める。

1. アプリ名とドメイン
2. 都道府県・国の入力方法 — 手動選択か、場所名からの自動推定か（MVPは手動選択で十分）
3. 世界地図SVGの調達先 — パブリックドメインのものを使い、ファイルサイズを100KB以下に簡略化する
4. 1回の旅行で複数国をまたぐ場合（周遊旅行）の通貨設定 — MVPは主要1通貨のみ。それ以外は円換算して入力
5. 旅行のアーカイブ機能の要否
6. アフィリエイト提携先の選定と申請 — 航空券（Skyscanner等）／国内宿（楽天トラベル）／海外宿（Booking.com、Agoda）のどこと組むか。開発と並行して申請する

---

## 付録A：確定した設計判断

| 論点 | 決定 | 理由 |
|---|---|---|
| コア機能 | 旅の記録 + 旅費管理 | 両方とも Trip 単位に紐づき、自然に統合できる |
| 利用者 | 自分 + 友人・家族 | 共有機能が必要 |
| 写真 | 旅行ごとに1枚（カバー） | Storage 無料枠とアップロード実装を大幅に節約 |
| 共有方式 | 招待リンク（セキュリティ強化版） | 参加ハードルが低い。トークンハッシュ化・期限・失効で安全性を担保 |
| 精算 | 記録と集計のみ | 支払者別合計まで出せば手動精算に足りる。将来拡張の余地は残す |
| 予約 | アプリ内完結はやらず、外部サイトへ送客リンクのみ | 旅行業登録・決済・在庫連携が不要。無料の記録アプリのまま成立し、アフィリエイトで収益化も可能 |
| 収益化とSupabase | 無料プランのまま収益化してよい | Supabaseは無料プランでの商用利用を明示的に許可。壁は規約ではなく容量・MAU・7日停止 |
| バックエンド | 既存Supabaseに相乗り（`travel` スキーマ） | 無料枠2プロジェクトを使い切っているため。追加コストゼロ |
| 海外対応 | MVPに含める | カラム追加はあとからだとデータ移行が発生するため、最初から入れる |
| 外貨レート | 旅行ごとに1レートを手入力 | 入力の手間が最小。帰国後にカード明細の実効レートで1箇所直せば全支出が正しくなる |
| 換算の注意書き | 円換算額の近くに常時表示 | カード会社の適用レート・手数料により実際の請求額と差が出るため |
| マップ | 日本地図と世界地図のタブ切替 | 都道府県と国、どちらの達成率も埋める楽しみを残す |
