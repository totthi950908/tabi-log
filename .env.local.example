# このファイルを .env.local にコピーして、値を実際のものに置き換えてください。
# .env.local は .gitignore 済みなので Git には上がりません。
#
# 値は推し活アプリと同じ Supabase プロジェクトのものを使います。
# Supabase ダッシュボード > Settings > API から取得できます。

VITE_SUPABASE_URL=https://xxxxxxxxxxxx.supabase.co
VITE_SUPABASE_ANON_KEY=eyJhbGci...(anon public キー)
